package main

//imports bring external packages into the file that u are working that u need 
import (
		"fmt" 
		"math"
)


func main() {
	//packageName.funcName --> call a function from a pacakage
	fmt.Println("sqrt(25)", math.Sqrt(25))
} 